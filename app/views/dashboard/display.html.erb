 <!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8" />
        <title>Impressive CSS3 Product Showcase | Tutorialzine Demo</title>
        
        <!-- Google Webfonts and our stylesheet -->
        <link rel="stylesheet" href="http://fonts.googleapis.com/css?family=PT+Sans+Narrow|Open+Sans:300" />
        <link rel="stylesheet" href="assets/css/styles.css" />
        
        <!--[if lt IE 9]>
          <script src="http://html5shiv.googlecode.com/svn/trunk/html5.js"></script>
        <![endif]-->
    </head>
    
    <body>

		<div id="impress" class="impress-not-supported">

	<% @photos.each_with_index do |p, i| %>
	<%# image = FastImage.size(p['src_big']) %>   
		<div class="step" style="width:<%= image[0] rescue '500px' %>px;height:<%= image[1] rescue '550px' %>px;border:#eee medium solid;background:url('assets/img/<%= cycle("frame1.JPG", "frame2.JPG") %>') no-repeat;overflow:none;" data-x="<%= 300*(i)+(rand(5)*50) %>" data-y="<%= (i*300)+(rand(5)*50) %>" data-scale="<%= 1 + rand() %>" data-rotate="<%= rand(360) %>">
			<div style="padding:15px;"> 
			<div style="position:relative;top:450px;background:white;opacity:0.7;border-radius:20px;padding:0px 12px;">
	        	<h2 style="color:black;"><%= p['caption'] %></h2> 
			</div>
				<div style="position:relative; left:11px;bottom:56px;width:448px;height:397px;overflow:none;background:url('<%= p['src_big'] %>') 50% 80% no-repeat;border:medium solid white;">
	 			<%#= image_tag(p['src_big']) %>
				</div>
			</div>

	    </div>   
	<% end %>
	<% @events.each_with_index do |e, i| %>  
		<% image = FastImage.size(e['pic']) %>
		<div class="step" style="width:500px;height:550px;border:grey thick solid;background:url('<%= e['pic'] %>')" data-x="<%= 300*(i)+(rand(5)*50) %>" data-y="<%= (i*300)+(rand(5)*50) %>" data-scale="<%= 1 + rand() %>" data-rotate="<%= rand(350) %>"> 
		<div class="transbox">
	        <h2><%= e['name'] %></h2> 
			<h3><%= '@' unless e['location'].blank? %> <%= e['location'] %> // <%= Time.at(e['start_time']).to_date.to_formatted_s(:long) %></h3>
			<p style="font-size:12px;"><%#= e['description'] %></p>     
		</div>
	   </div>   
	<% end %>  
  
		</div>

		<a id="arrowLeft" class="arrow">&lt;</a>
		<a id="arrowRight" class="arrow">&gt;</a>

        <footer>
	        <h2>PROJECT NAME</h2>
            <a class="tzine" href="#">Created by awesome people.</a>
        </footer>
        
        <!-- JavaScript includes -->
		<script src="http://code.jquery.com/jquery-1.7.1.min.js"></script>
		<script src="assets/js/impress.js"></script>
		<script src="assets/js/script.js"></script>

    </body>
</html>

